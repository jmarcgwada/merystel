
'use client';

import React from 'react';

export function SessionManager({ children }: { children: React.ReactNode }) {
  // Session management logic is disabled as Firebase connection is removed.
  return <>{children}</>;
}
