/**
 * @fileoverview Firestore Security Rules for Zenith POS.
 *
 * Core Philosophy:
 * This ruleset enforces a strict company-based data isolation model. All data
 * is scoped to a specific company, and only authenticated users are allowed
 * access. Access to company-specific data is implicitly granted to any
 * authenticated user. User-specific data (profiles) are accessible only to the
 * owning user. Public listing of collections is generally disallowed unless
 * explicitly intended for public consumption.
 *
 * Data Structure:
 * - /users/{userId}: Stores individual user profiles.
 * - /companies/{companyId}: Stores company-level data.
 * - All other collections are subcollections of /companies/{companyId}.
 *
 * Key Security Decisions:
 * - Users can only read/write their own user document.
 * - Any authenticated user can read and write all company data.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Allows a user to read and write their own user profile.
     * @path /users/{userId}
     * @allow (get, create, update, delete) if the user is signed in and the requested user ID matches their authentication UID.
     * @deny (get, create, update, delete) if the user is not signed in or the requested user ID does not match their authentication UID.
     * @principle Enforces document ownership for user profiles.
     */
    match /users/{userId} {
      function isSignedIn() {
        return request.auth != null;
      }
      function isOwner(userId) {
        return request.auth.uid == userId;
      }
      function isExistingOwner(userId) {
        return isOwner(userId) && resource != null;
      }

      allow get: if isOwner(userId);
      allow list: if false;

      allow create: if isSignedIn() && isOwner(userId);
      allow update: if isSignedIn() && isExistingOwner(userId);
      allow delete: if isSignedIn() && isExistingOwner(userId);
    }

    /**
     * @description Allows any authenticated user to read and write company data.
     * @path /companies/{companyId}
     * @allow (get, create, update, delete) if the user is signed in.
     * @deny (get, create, update, delete) if the user is not signed in.
     * @principle Grants authenticated users access to company data.
     */
    match /companies/{companyId} {
      function isSignedIn() {
        return request.auth != null;
      }
      allow get: if isSignedIn();
      allow list: if false;
      allow create: if isSignedIn();
      allow update: if isSignedIn();
      allow delete: if isSignedIn();

       /**
         * @description Allows any authenticated user to read and write VAT rates.
         * @path /companies/{companyId}/vatRates/{vatId}
         * @allow (get, list, create, update, delete) if the user is signed in.
         * @deny (get, list, create, update, delete) if the user is not signed in.
         * @principle Grants authenticated users access to company VAT rates.
         */
      match /vatRates/{vatId} {
        function isSignedIn() {
          return request.auth != null;
        }
        allow get: if isSignedIn();
        allow list: if isSignedIn();
        allow create: if isSignedIn();
        allow update: if isSignedIn();
        allow delete: if isSignedIn();
      }


      /**
       * @description Allows any authenticated user to read and write customer data within a company.
       * @path /companies/{companyId}/customers/{customerId}
       * @allow (get, list, create, update, delete) if the user is signed in.
       * @deny (get, list, create, update, delete) if the user is not signed in.
       * @principle Grants authenticated users access to company customer data.
       */
      match /customers/{customerId} {
        function isSignedIn() {
          return request.auth != null;
        }
        allow get: if isSignedIn();
        allow list: if isSignedIn();
        allow create: if isSignedIn();
        allow update: if isSignedIn();
        allow delete: if isSignedIn();
      }

      /**
       * @description Allows any authenticated user to read and write supplier data within a company.
       * @path /companies/{companyId}/suppliers/{supplierId}
       * @allow (get, list, create, update, delete) if the user is signed in.
       * @deny (get, list, create, update, delete) if the user is not signed in.
       * @principle Grants authenticated users access to company supplier data.
       */
      match /suppliers/{supplierId} {
        function isSignedIn() {
          return request.auth != null;
        }
        allow get: if isSignedIn();
        allow list: if isSignedIn();
        allow create: if isSignedIn();
        allow update: if isSignedIn();
        allow delete: if isSignedIn();
      }

      /**
       * @description Allows any authenticated user to read and write categories within a company.
       * @path /companies/{companyId}/categories/{categoryId}
       * @allow (get, list, create, update, delete) if the user is signed in.
       * @deny (get, list, create, update, delete) if the user is not signed in.
       * @principle Grants authenticated users access to company categories.
       */
      match /categories/{categoryId} {
        function isSignedIn() {
          return request.auth != null;
        }
        allow get: if isSignedIn();
        allow list: if isSignedIn();
        allow create: if isSignedIn();
        allow update: if isSignedIn();
        allow delete: if isSignedIn();
      }

      /**
       * @description Allows any authenticated user to read and write items within a company.
       * @path /companies/{companyId}/items/{itemId}
       * @allow (get, list, create, update, delete) if the user is signed in.
       * @deny (get, list, create, update, delete) if the user is not signed in.
       * @principle Grants authenticated users access to company items.
       */
      match /items/{itemId} {
        function isSignedIn() {
          return request.auth != null;
        }
        allow get: if isSignedIn();
        allow list: if isSignedIn();
        allow create: if isSignedIn();
        allow update: if isSignedIn();
        allow delete: if isSignedIn();
      }

      /**
       * @description Allows any authenticated user to read and write tables within a company.
       * @path /companies/{companyId}/tables/{tableId}
       * @allow (get, list, create, update, delete) if the user is signed in.
       * @deny (get, list, create, update, delete) if the user is not signed in.
       * @principle Grants authenticated users access to company tables.
       */
      match /tables/{tableId} {
        function isSignedIn() {
          return request.auth != null;
        }
        allow get: if isSignedIn();
        allow list: if isSignedIn();
        allow create: if isSignedIn();
        allow update: if isSignedIn();
        allow delete: if isSignedIn();
      }

      /**
       * @description Allows any authenticated user to read and write sales data within a company.
       * @path /companies/{companyId}/sales/{saleId}
       * @allow (get, list, create, update, delete) if the user is signed in.
       * @deny (get, list, create, update, delete) if the user is not signed in.
       * @principle Grants authenticated users access to company sales data.
       */
      match /sales/{saleId} {
        function isSignedIn() {
          return request.auth != null;
        }
        allow get: if isSignedIn();
        allow list: if isSignedIn();
        allow create: if isSignedIn();
        allow update: if isSignedIn();
        allow delete: if isSignedIn();
      }

      /**
       * @description Allows any authenticated user to read and write payment methods within a company.
       * @path /companies/{companyId}/paymentMethods/{methodId}
       * @allow (get, list, create, update, delete) if the user is signed in.
       * @deny (get, list, create, update, delete) if the user is not signed in.
       * @principle Grants authenticated users access to company payment methods.
       */
      match /paymentMethods/{methodId} {
        function isSignedIn() {
          return request.auth != null;
        }
        allow get: if isSignedIn();
        allow list: if isSignedIn();
        allow create: if isSignedIn();
        allow update: if isSignedIn();
        allow delete: if isSignedIn();
      }

      /**
       * @description Allows any authenticated user to read and write held orders within a company.
       * @path /companies/{companyId}/heldOrders/{orderId}
       * @allow (get, list, create, update, delete) if the user is signed in.
       * @deny (get, list, create, update, delete) if the user is not signed in.
       * @principle Grants authenticated users access to company held orders.
       */
      match /heldOrders/{orderId} {
        function isSignedIn() {
          return request.auth != null;
        }
        allow get: if isSignedIn();
        allow list: if isSignedIn();
        allow create: if isSignedIn();
        allow update: if isSignedIn();
        allow delete: if isSignedIn();
      }
    }
  }
}