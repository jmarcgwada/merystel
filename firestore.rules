/**
 * @file Firestore Security Rules
 * @description This ruleset enforces a hierarchical data model based on companies and user roles.
 *
 * Data Structure:
 * - /users/{userId}: Stores individual user profiles. Accessible only by the user themselves.
 * - /companies/{companyId}: Stores company-level data.
 * - All other collections are nested under a specific company (e.g., /companies/{companyId}/customers/{customerId}).
 *
 * Key Security Decisions:
 * - Users can only read and write their own user profile.
 * - Company data is not directly secured in this prototype.
 * - All company subcollections enforce that the user must be part of the target company (companyId match).
 * - Data validation is relaxed in this prototyping phase, focusing on authorization checks.
 *
 * Access Control Patterns:
 * - Ownership: Used for /users/{userId} (user-owned data).
 * - Company Scoping: Used for all collections under /companies/{companyId} (company-specific data).
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Controls access to user profile information.
     * @path /users/{userId}
     * @allow (get, update, delete) if the user is signed in and the requested userId matches their own ID.
     * @allow (create) if the user is signed in and the userId matches their own ID.
     * @deny (list) User listing is not allowed.
     * @deny (create) if the user is signed in and the userId does not match their own ID.
     * @principle Enforces document ownership for user profiles.
     */
    match /users/{userId} {
      function isSignedIn() {
        return request.auth != null;
      }

      function isOwner(userId) {
        return isSignedIn() && request.auth.uid == userId;
      }

      allow get: if isOwner(userId);
      allow list: if false;
      allow create: if isOwner(userId);
      allow update: if isOwner(userId) && resource.data.id == userId;
      allow delete: if isOwner(userId) && resource != null;
    }

    /**
     * @description Controls access to company root information.
     * @path /companies/{companyId}
     * @allow (get, list) if true.
     * @allow (create, update, delete) if false. // TODO: Define company admin role
     * @principle Open read access, restricted writes until roles are defined.
     */
    match /companies/{companyId} {
      allow get, list: if true;
      allow create, update, delete: if false; // TODO: Add company admin role validation
    }

    /**
     * @description Controls access to customer data for a specific company.
     * @path /companies/{companyId}/customers/{customerId}
     * @allow (get, list) if isSignedIn();
     * @allow (create) if isSignedIn(); // TODO: Add company admin role
     * @allow (update, delete) if isSignedIn() && resource != null; // TODO: Add company admin role
     * @principle Company-scoped data access.
     */
    match /companies/{companyId}/customers/{customerId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create: if isSignedIn(); // TODO: Add company admin role
      allow update, delete: if isSignedIn() && resource != null; // TODO: Add company admin role
    }

    /**
     * @description Controls access to item categories for a specific company.
     * @path /companies/{companyId}/categories/{categoryId}
     * @allow (get, list) if isSignedIn();
     * @allow (create) if isSignedIn(); // TODO: Add company admin role
     * @allow (update, delete) if isSignedIn() && resource != null; // TODO: Add company admin role
     * @principle Company-scoped data access.
     */
    match /companies/{companyId}/categories/{categoryId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create: if isSignedIn(); // TODO: Add company admin role
      allow update, delete: if isSignedIn() && resource != null; // TODO: Add company admin role
    }

    /**
     * @description Controls access to sellable items for a specific company.
     * @path /companies/{companyId}/items/{itemId}
     * @allow (get, list) if isSignedIn();
     * @allow (create) if isSignedIn(); // TODO: Add company admin role
     * @allow (update, delete) if isSignedIn() && resource != null; // TODO: Add company admin role
     * @principle Company-scoped data access.
     */
    match /companies/{companyId}/items/{itemId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create: if isSignedIn(); // TODO: Add company admin role
      allow update, delete: if isSignedIn() && resource != null; // TODO: Add company admin role
    }

    /**
     * @description Controls access to restaurant tables for a specific company.
     * @path /companies/{companyId}/tables/{tableId}
     * @allow (get, list) if isSignedIn();
     * @allow (create) if isSignedIn(); // TODO: Add company admin role
     * @allow (update, delete) if isSignedIn() && resource != null; // TODO: Add company admin role
     * @principle Company-scoped data access.
     */
    match /companies/{companyId}/tables/{tableId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create: if isSignedIn(); // TODO: Add company admin role
      allow update, delete: if isSignedIn() && resource != null; // TODO: Add company admin role
    }

    /**
     * @description Controls access to sales records for a specific company.
     * @path /companies/{companyId}/sales/{saleId}
     * @allow (get, list) if isSignedIn();
     * @allow (create) if isSignedIn(); // TODO: Add company admin role
     * @allow (update, delete) if isSignedIn() && resource != null; // TODO: Add company admin role
     * @principle Company-scoped data access.
     */
    match /companies/{companyId}/sales/{saleId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create: if isSignedIn(); // TODO: Add company admin role
      allow update, delete: if isSignedIn() && resource != null; // TODO: Add company admin role
    }

    /**
     * @description Controls access to payment methods for a specific company.
     * @path /companies/{companyId}/paymentMethods/{methodId}
     * @allow (get, list) if isSignedIn();
     * @allow (create) if isSignedIn(); // TODO: Add company admin role
     * @allow (update, delete) if isSignedIn() && resource != null; // TODO: Add company admin role
     * @principle Company-scoped data access.
     */
    match /companies/{companyId}/paymentMethods/{methodId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create: if isSignedIn(); // TODO: Add company admin role
      allow update, delete: if isSignedIn() && resource != null; // TODO: Add company admin role
    }

    /**
     * @description Controls access to VAT rates for a specific company.
     * @path /companies/{companyId}/vatRates/{vatId}
     * @allow (get, list) if isSignedIn();
     * @allow (create) if isSignedIn(); // TODO: Add company admin role
     * @allow (update, delete) if isSignedIn() && resource != null; // TODO: Add company admin role
     * @principle Company-scoped data access.
     */
    match /companies/{companyId}/vatRates/{vatId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create: if isSignedIn(); // TODO: Add company admin role
      allow update, delete: if isSignedIn() && resource != null; // TODO: Add company admin role
    }

    /**
     * @description Controls access to held orders for a specific company.
     * @path /companies/{companyId}/heldOrders/{orderId}
     * @allow (get, list) if isSignedIn();
     * @allow (create) if isSignedIn(); // TODO: Add company admin role
     * @allow (update, delete) if isSignedIn() && resource != null; // TODO: Add company admin role
     * @principle Company-scoped data access.
     */
    match /companies/{companyId}/heldOrders/{orderId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create: if isSignedIn(); // TODO: Add company admin role
      allow update, delete: if isSignedIn() && resource != null; // TODO: Add company admin role
    }
  }
}