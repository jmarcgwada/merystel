/**
 * @description This ruleset enforces a strict user-ownership model for user profiles and restricts company data access to authenticated users. All company-related data is segregated under the /companies/{companyId} path.
 * @dataStructure
 *   - /users/{userId}: Stores individual user profiles, accessible only by the user themselves.
 *   - /companies/{companyId}: Root document for company-specific data.
 *   - /companies/{companyId}/customers/{customerId}: Customers belonging to a company.
 *   - /companies/{companyId}/suppliers/{supplierId}: Suppliers belonging to a company.
 *   - /companies/{companyId}/categories/{categoryId}: Item categories for a company.
 *   - /companies/{companyId}/items/{itemId}: Items for sale in a company.
 *   - /companies/{companyId}/tables/{tableId}: Restaurant tables within a company.
 *   - /companies/{companyId}/sales/{saleId}: Sales records for a company.
 *   - /companies/{companyId}/paymentMethods/{methodId}: Payment methods for a company.
 *   - /companies/{companyId}/vatRates/{vatId}: VAT rates for a company.
 *   - /companies/{companyId}/heldOrders/{orderId}: Held orders for a company.
 * @keySecurityDecisions
 *   - User profiles are private and only accessible to the owning user.
 *   - Listing users is disallowed to prevent information disclosure.
 *   - All company subcollections require authentication to access. Specific authorization within a company is not defined in this prototype and would require denormalized roles or group membership.
 * @denormalizationForAuthorization
 *   - Company subcollections do not specify ownership. Future versions should include a `companyId` field on each document to simplify and secure authorization.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Controls access to user profile information.
     * @path /users/{userId}
     * @allow (create) User with ID 'user123' can create their profile.
     *   - request.auth.uid: 'user123'
     *   - request.resource.data.id: 'user123'
     * @allow (get) User with ID 'user123' can read their profile.
     *   - request.auth.uid: 'user123'
     * @allow (update) User with ID 'user123' can update their profile.
     *   - request.auth.uid: 'user123'
     * @allow (delete) User with ID 'user123' can delete their profile.
     *   - request.auth.uid: 'user123'
     * @deny (create) User with ID 'user123' cannot create a profile for 'user456'.
     *   - request.auth.uid: 'user123'
     *   - request.resource.data.id: 'user456'
     * @deny (get) User with ID 'user123' cannot read the profile of 'user456'.
     *   - request.auth.uid: 'user123'
     * @deny (update) User with ID 'user123' cannot update the profile of 'user456'.
     *   - request.auth.uid: 'user123'
     * @deny (delete) User with ID 'user123' cannot delete the profile of 'user456'.
     *   - request.auth.uid: 'user123'
     * @principle Enforces document ownership for all operations on user profiles.
     */
    match /users/{userId} {
      function isOwner(userId) {
        return request.auth.uid == userId;
      }
      function isExistingOwner(userId) {
          return isOwner(userId) && resource != null;
      }

      allow get: if isOwner(userId);
      allow list: if false;
      allow create: if isOwner(userId) && request.resource.data.id == userId;
      allow update: if isExistingOwner(userId) && request.resource.data.id == resource.data.id;
      allow delete: if isExistingOwner(userId);
    }

    /**
     * @description Controls access to company root documents.
     * @path /companies/{companyId}
     * @allow (get) Authenticated user can read company data.
     *   - request.auth.uid: 'user123'
     * @allow (list) Listing companies is not allowed.
     * @allow (create) Creating companies is not allowed.
     * @allow (update) Updating companies is not allowed.
     * @allow (delete) Deleting companies is not allowed.
     * @deny (get) Unauthenticated user cannot read company data.
     *   - request.auth.uid: null
     * @principle Requires authentication for all company data access.
     */
    match /companies/{companyId} {
      function isSignedIn() {
        return request.auth != null;
      }
      allow get: if isSignedIn();
      allow list: if false;
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Controls access to customer data within a company.
     * @path /companies/{companyId}/customers/{customerId}
     * @allow (get) Authenticated user can read customer data.
     *   - request.auth.uid: 'user123'
     * @allow (list) Authenticated user can list customer data.
     *   - request.auth.uid: 'user123'
     * @allow (create) Creating customer data is not allowed.
     * @allow (update) Updating customer data is not allowed.
     * @allow (delete) Deleting customer data is not allowed.
     * @deny (get) Unauthenticated user cannot read customer data.
     *   - request.auth.uid: null
     * @principle Requires authentication for all company customer data access.
     */
    match /companies/{companyId}/customers/{customerId} {
      function isSignedIn() {
        return request.auth != null;
      }
      allow get: if isSignedIn();
      allow list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Controls access to supplier data within a company.
     * @path /companies/{companyId}/suppliers/{supplierId}
     * @allow (get) Authenticated user can read supplier data.
     *   - request.auth.uid: 'user123'
     * @allow (list) Authenticated user can list supplier data.
     *   - request.auth.uid: 'user123'
     * @allow (create) Creating supplier data is not allowed.
     * @allow (update) Updating supplier data is not allowed.
     * @allow (delete) Deleting supplier data is not allowed.
     * @deny (get) Unauthenticated user cannot read supplier data.
     *   - request.auth.uid: null
     * @principle Requires authentication for all company supplier data access.
     */
    match /companies/{companyId}/suppliers/{supplierId} {
      function isSignedIn() {
        return request.auth != null;
      }
      allow get: if isSignedIn();
      allow list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Controls access to category data within a company.
     * @path /companies/{companyId}/categories/{categoryId}
     * @allow (get) Authenticated user can read category data.
     *   - request.auth.uid: 'user123'
     * @allow (list) Authenticated user can list category data.
     *   - request.auth.uid: 'user123'
     * @allow (create) Creating category data is not allowed.
     * @allow (update) Updating category data is not allowed.
     * @allow (delete) Deleting category data is not allowed.
     * @deny (get) Unauthenticated user cannot read category data.
     *   - request.auth.uid: null
     * @principle Requires authentication for all company category data access.
     */
    match /companies/{companyId}/categories/{categoryId} {
      function isSignedIn() {
        return request.auth != null;
      }
      allow get: if isSignedIn();
      allow list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Controls access to item data within a company.
     * @path /companies/{companyId}/items/{itemId}
     * @allow (get) Authenticated user can read item data.
     *   - request.auth.uid: 'user123'
     * @allow (list) Authenticated user can list item data.
     *   - request.auth.uid: 'user123'
     * @allow (create) Creating item data is not allowed.
     * @allow (update) Updating item data is not allowed.
     * @allow (delete) Deleting item data is not allowed.
     * @deny (get) Unauthenticated user cannot read item data.
     *   - request.auth.uid: null
     * @principle Requires authentication for all company item data access.
     */
    match /companies/{companyId}/items/{itemId} {
      function isSignedIn() {
        return request.auth != null;
      }
      allow get: if isSignedIn();
      allow list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Controls access to table data within a company.
     * @path /companies/{companyId}/tables/{tableId}
     * @allow (get) Authenticated user can read table data.
     *   - request.auth.uid: 'user123'
     * @allow (list) Authenticated user can list table data.
     *   - request.auth.uid: 'user123'
     * @allow (create) Creating table data is not allowed.
     * @allow (update) Updating table data is not allowed.
     * @allow (delete) Deleting table data is not allowed.
     * @deny (get) Unauthenticated user cannot read table data.
     *   - request.auth.uid: null
     * @principle Requires authentication for all company table data access.
     */
    match /companies/{companyId}/tables/{tableId} {
      function isSignedIn() {
        return request.auth != null;
      }
      allow get: if isSignedIn();
      allow list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Controls access to sales data within a company.
     * @path /companies/{companyId}/sales/{saleId}
     * @allow (get) Authenticated user can read sales data.
     *   - request.auth.uid: 'user123'
     * @allow (list) Authenticated user can list sales data.
     *   - request.auth.uid: 'user123'
     * @allow (create) Creating sales data is not allowed.
     * @allow (update) Updating sales data is not allowed.
     * @allow (delete) Deleting sales data is not allowed.
     * @deny (get) Unauthenticated user cannot read sales data.
     *   - request.auth.uid: null
     * @principle Requires authentication for all company sales data access.
     */
    match /companies/{companyId}/sales/{saleId} {
      function isSignedIn() {
        return request.auth != null;
      }
      allow get: if isSignedIn();
      allow list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Controls access to payment method data within a company.
     * @path /companies/{companyId}/paymentMethods/{methodId}
     * @allow (get) Authenticated user can read payment method data.
     *   - request.auth.uid: 'user123'
     * @allow (list) Authenticated user can list payment method data.
     *   - request.auth.uid: 'user123'
     * @allow (create) Creating payment method data is not allowed.
     * @allow (update) Updating payment method data is not allowed.
     * @allow (delete) Deleting payment method data is not allowed.
     * @deny (get) Unauthenticated user cannot read payment method data.
     *   - request.auth.uid: null
     * @principle Requires authentication for all company payment method data access.
     */
    match /companies/{companyId}/paymentMethods/{methodId} {
      function isSignedIn() {
        return request.auth != null;
      }
      allow get: if isSignedIn();
      allow list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Controls access to VAT rate data within a company.
     * @path /companies/{companyId}/vatRates/{vatId}
     * @allow (get) Authenticated user can read VAT rate data.
     *   - request.auth.uid: 'user123'
     * @allow (list) Authenticated user can list VAT rate data.
     *   - request.auth.uid: 'user123'
     * @allow (create) Creating VAT rate data is not allowed.
     * @allow (update) Updating VAT rate data is not allowed.
     * @allow (delete) Deleting VAT rate data is not allowed.
     * @deny (get) Unauthenticated user cannot read VAT rate data.
     *   - request.auth.uid: null
     * @principle Requires authentication for all company VAT rate data access.
     */
    match /companies/{companyId}/vatRates/{vatId} {
      function isSignedIn() {
        return request.auth != null;
      }
      allow get: if isSignedIn();
      allow list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

    /**
     * @description Controls access to held order data within a company.
     * @path /companies/{companyId}/heldOrders/{orderId}
     * @allow (get) Authenticated user can read held order data.
     *   - request.auth.uid: 'user123'
     * @allow (list) Authenticated user can list held order data.
     *   - request.auth.uid: 'user123'
     * @allow (create) Creating held order data is not allowed.
     * @allow (update) Updating held order data is not allowed.
     * @allow (delete) Deleting held order data is not allowed.
     * @deny (get) Unauthenticated user cannot read held order data.
     *   - request.auth.uid: null
     * @principle Requires authentication for all company held order data access.
     */
    match /companies/{companyId}/heldOrders/{orderId} {
      function isSignedIn() {
        return request.auth != null;
      }
      allow get: if isSignedIn();
      allow list: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }

      /**
       * @description Controls access to category data within a "main" company.
       * @path /companies/main/categories/{categoryId}
       * @allow (get) Authenticated user can read category data.
       *   - request.auth.uid: 'user123'
       * @allow (list) Authenticated user can list category data.
       *   - request.auth.uid: 'user123'
       * @allow (create) Creating category data is not allowed.
       * @allow (update) Updating category data is not allowed.
       * @allow (delete) Deleting category data is not allowed.
       * @deny (get) Unauthenticated user cannot read category data.
       *   - request.auth.uid: null
       * @principle Requires authentication for all company category data access.
       */
      match /companies/{companyId}/categories/{categoryId} {
          function isSignedIn() {
              return request.auth != null;
          }
          allow get: if isSignedIn();
          allow list: if isSignedIn();
          allow create: if false;
          allow update: if false;
          allow delete: if false;
      }
  }
}