rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Controls access to user profile information.
     * @path /users/{userId}
     * @allow (create) - Authenticated user can create their own profile if the userId matches their auth UID.
     * @allow (get, update, delete) - Authenticated user can access and modify their own profile if the userId matches their auth UID.
     * @deny (list) - Listing all users is not allowed.
     * @deny (create, update, delete) - If the userId does not match the authenticated user's UID.
     * @principle Enforces document ownership for user profiles.
     */
    match /users/{userId} {
      function isSignedIn() {
        return request.auth != null;
      }

      function isOwner(userId) {
        return request.auth.uid == userId;
      }

      function isExistingOwner(userId) {
        return isOwner(userId) && exists(resource);
      }

      allow get: if isOwner(userId);
      allow list: if false;
      allow create: if isSignedIn() && isOwner(userId);
      allow update: if isExistingOwner(userId);
      allow delete: if isExistingOwner(userId);
    }

    /**
     * @description Controls access to company information.
     * @path /companies/{companyId}
     * @allow (get, list) - Any authenticated user can read company information.
     * @allow (create, update, delete) - Any authenticated user can modify company information.
     * @principle Allows authenticated users to manage company data.
     */
    match /companies/{companyId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create, update, delete: if isSignedIn();
    }

    /**
     * @description Controls access to customer data for a specific company.
     * @path /companies/{companyId}/customers/{customerId}
     * @allow (get, list) - Any authenticated user can read customer data within a company.
     * @allow (create, update, delete) - Any authenticated user can manage customer data within a company.
     * @principle Allows authenticated users to manage company customer data.
     */
    match /companies/{companyId}/customers/{customerId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create, update, delete: if isSignedIn();
    }

    /**
     * @description Controls access to supplier data for a specific company.
     * @path /companies/{companyId}/suppliers/{supplierId}
     * @allow (get, list) - Any authenticated user can read supplier data within a company.
     * @allow (create, update, delete) - Any authenticated user can manage supplier data within a company.
     * @principle Allows authenticated users to manage company supplier data.
     */
    match /companies/{companyId}/suppliers/{supplierId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create, update, delete: if isSignedIn();
    }

    /**
     * @description Controls access to item categories for a specific company.
     * @path /companies/{companyId}/categories/{categoryId}
     * @allow (get, list) - Any authenticated user can read category data within a company.
     * @allow (create, update, delete) - Any authenticated user can manage category data within a company.
     * @principle Allows authenticated users to manage company category data.
     */
    match /companies/{companyId}/categories/{categoryId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create, update, delete: if isSignedIn();
    }

    /**
     * @description Controls access to items for a specific company.
     * @path /companies/{companyId}/items/{itemId}
     * @allow (get, list) - Any authenticated user can read item data within a company.
     * @allow (create, update, delete) - Any authenticated user can manage item data within a company.
     * @principle Allows authenticated users to manage company item data.
     */
    match /companies/{companyId}/items/{itemId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create, update, delete: if isSignedIn();
    }

    /**
     * @description Controls access to tables for a specific company.
     * @path /companies/{companyId}/tables/{tableId}
     * @allow (get, list) - Any authenticated user can read table data within a company.
     * @allow (create, update, delete) - Any authenticated user can manage table data within a company.
     * @principle Allows authenticated users to manage company table data.
     */
    match /companies/{companyId}/tables/{tableId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create, update, delete: if isSignedIn();
    }

    /**
     * @description Controls access to sales data for a specific company.
     * @path /companies/{companyId}/sales/{saleId}
     * @allow (get, list) - Any authenticated user can read sales data within a company.
     * @allow (create, update, delete) - Any authenticated user can manage sales data within a company.
     * @principle Allows authenticated users to manage company sales data.
     */
    match /companies/{companyId}/sales/{saleId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create, update, delete: if isSignedIn();
    }

    /**
     * @description Controls access to payment methods for a specific company.
     * @path /companies/{companyId}/paymentMethods/{methodId}
     * @allow (get, list) - Any authenticated user can read payment method data within a company.
     * @allow (create, update, delete) - Any authenticated user can manage payment method data within a company.
     * @principle Allows authenticated users to manage company payment method data.
     */
    match /companies/{companyId}/paymentMethods/{methodId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create, update, delete: if isSignedIn();
    }

    /**
     * @description Controls access to VAT rates for a specific company.
     * @path /companies/{companyId}/vatRates/{vatId}
     * @allow (get, list) - Any authenticated user can read VAT rate data within a company.
     * @allow (create, update, delete) - Any authenticated user can manage company VAT rate data.
     * @principle Allows authenticated users to manage company VAT rate data.
     */
    match /companies/{companyId}/vatRates/{vatId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create, update, delete: if isSignedIn();
    }

    /**
     * @description Controls access to held orders for a specific company.
     * @path /companies/{companyId}/heldOrders/{orderId}
     * @allow (get, list) - Any authenticated user can read held order data within a company.
     * @allow (create, update, delete) - Any authenticated user can manage held order data within a company.
     * @principle Allows authenticated users to manage company held order data.
     */
    match /companies/{companyId}/heldOrders/{orderId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create, update, delete: if isSignedIn();
    }
  }
}