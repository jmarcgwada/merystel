/**
 * @fileoverview Firestore Security Rules for Zenith POS.
 *
 * Core Philosophy:
 * This ruleset enforces a hierarchical data ownership model based on companies and users.
 * Each company owns its data (customers, suppliers, items, etc.), and users have individual profiles.
 * Rules are designed to prevent unauthorized data access and modification.
 *
 * Data Structure:
 * - /users/{userId}: Stores individual user profiles. Accessible only by the user.
 * - /companies/{companyId}: Stores company-level data.
 * - /companies/{companyId}/[...]: Subcollections under a company are accessible only to authenticated users.
 *
 * Key Security Decisions:
 * - Users can only read and write their own user documents.
 * - All company-related data is secured and requires authentication.
 * - Schema validation is relaxed in prototyping mode to allow for rapid data model iteration.
 * - List operations are generally restricted to owners of user-scoped data or denied where private data would be exposed.
 *
 * Denormalization for Authorization:
 *  N/A - In this iteration, the rules rely on path-based authorization and the authenticated user's ID. More complex scenarios might benefit from denormalization.
 *
 * Structural Segregation:
 * N/A - Drafts vs. published content is not explicitly defined in the current data model.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Secure user profiles. Only the authenticated user can read/write their own profile.
     * @path /users/{userId}
     * @allow (get, update, delete) User with matching ID can access their own data.
     * @allow (create) User can create their own profile if the userId matches their auth.uid.
     * @deny (get, update, delete) User attempts to access another user's profile.
     * @principle Enforces document ownership for writes.
     */
    match /users/{userId} {
      function isSignedIn() {
        return request.auth != null;
      }

      function isOwner(userId) {
        return request.auth.uid == userId;
      }

      function isExistingOwner(userId) {
        return isOwner(userId) && resource != null;
      }

      allow get: if isSignedIn() && isOwner(userId);
      allow list: if false;
      allow create: if isSignedIn() && isOwner(userId);
      allow update: if isSignedIn() && isExistingOwner(userId);
      allow delete: if isSignedIn() && isExistingOwner(userId);
    }

    /**
     * @description Secure company root documents. Authenticated users can read company data.
     * @path /companies/{companyId}
     * @allow (get, list) Authenticated user can read company data.
     * @deny (create, update, delete) No one can create, update, or delete company root documents through the API.
     * @principle Requires authentication for data access.
     */
    match /companies/{companyId} {
        function isSignedIn() {
            return request.auth != null;
        }
        allow get: if isSignedIn();
        allow list: if false;
        allow create: if false;
        allow update: if false;
        allow delete: if false;
    }

    /**
     * @description Secure customer data for a specific company. Authenticated users can read and write customer data.
     * @path /companies/{companyId}/customers/{customerId}
     * @allow (get, list, create, update, delete) Authenticated user can access customer data.
     * @principle Requires authentication for data access.
     */
    match /companies/{companyId}/customers/{customerId} {
        function isSignedIn() {
            return request.auth != null;
        }
        allow get: if isSignedIn();
        allow list: if isSignedIn();
        allow create: if isSignedIn();
        allow update: if isSignedIn();
        allow delete: if isSignedIn();
    }

    /**
     * @description Secure supplier data for a specific company. Authenticated users can read and write supplier data.
     * @path /companies/{companyId}/suppliers/{supplierId}
     * @allow (get, list, create, update, delete) Authenticated user can access supplier data.
     * @principle Requires authentication for data access.
     */
    match /companies/{companyId}/suppliers/{supplierId} {
        function isSignedIn() {
            return request.auth != null;
        }
        allow get: if isSignedIn();
        allow list: if isSignedIn();
        allow create: if isSignedIn();
        allow update: if isSignedIn();
        allow delete: if isSignedIn();
    }

    /**
     * @description Secure item categories for a specific company. Authenticated users can read and write category data.
     * @path /companies/{companyId}/categories/{categoryId}
     * @allow (get, list, create, update, delete) Authenticated user can access category data.
     * @principle Requires authentication for data access.
     */
    match /companies/{companyId}/categories/{categoryId} {
        function isSignedIn() {
            return request.auth != null;
        }
        allow get: if isSignedIn();
        allow list: if isSignedIn();
        allow create: if isSignedIn();
        allow update: if isSignedIn();
        allow delete: if isSignedIn();
    }

    /**
     * @description Secure items for a specific company. Authenticated users can read and write item data.
     * @path /companies/{companyId}/items/{itemId}
     * @allow (get, list, create, update, delete) Authenticated user can access item data.
     * @principle Requires authentication for data access.
     */
    match /companies/{companyId}/items/{itemId} {
        function isSignedIn() {
            return request.auth != null;
        }
        allow get: if isSignedIn();
        allow list: if isSignedIn();
        allow create: if isSignedIn();
        allow update: if isSignedIn();
        allow delete: if isSignedIn();
    }

    /**
     * @description Secure restaurant tables for a specific company. Authenticated users can read and write table data.
     * @path /companies/{companyId}/tables/{tableId}
     * @allow (get, list, create, update, delete) Authenticated user can access table data.
     * @principle Requires authentication for data access.
     */
    match /companies/{companyId}/tables/{tableId} {
        function isSignedIn() {
            return request.auth != null;
        }
        allow get: if isSignedIn();
        allow list: if isSignedIn();
        allow create: if isSignedIn();
        allow update: if isSignedIn();
        allow delete: if isSignedIn();
    }

    /**
     * @description Secure sales data for a specific company. Authenticated users can read and write sales data.
     * @path /companies/{companyId}/sales/{saleId}
     * @allow (get, list, create, update, delete) Authenticated user can access sales data.
     * @principle Requires authentication for data access.
     */
    match /companies/{companyId}/sales/{saleId} {
        function isSignedIn() {
            return request.auth != null;
        }
        allow get: if isSignedIn();
        allow list: if isSignedIn();
        allow create: if isSignedIn();
        allow update: if isSignedIn();
        allow delete: if isSignedIn();
    }

    /**
     * @description Secure payment methods for a specific company. Authenticated users can read and write payment method data.
     * @path /companies/{companyId}/paymentMethods/{methodId}
     * @allow (get, list, create, update, delete) Authenticated user can access payment method data.
     * @principle Requires authentication for data access.
     */
    match /companies/{companyId}/paymentMethods/{methodId} {
        function isSignedIn() {
            return request.auth != null;
        }
        allow get: if isSignedIn();
        allow list: if isSignedIn();
        allow create: if isSignedIn();
        allow update: if isSignedIn();
        allow delete: if isSignedIn();
    }

    /**
     * @description Secure VAT rates for a specific company. Authenticated users can read and write VAT rate data.
     * @path /companies/{companyId}/vatRates/{vatId}
     * @allow (get, list, create, update, delete) Authenticated user can access VAT rate data.
     * @principle Requires authentication for data access.
     */
    match /companies/{companyId}/vatRates/{vatId} {
        function isSignedIn() {
            return request.auth != null;
        }
        allow get: if isSignedIn();
        allow list: if isSignedIn();
        allow create: if isSignedIn();
        allow update: if isSignedIn();
        allow delete: if isSignedIn();
    }

    /**
     * @description Secure held orders for a specific company. Authenticated users can read and write held order data.
     * @path /companies/{companyId}/heldOrders/{orderId}
     * @allow (get, list, create, update, delete) Authenticated user can access held order data.
     * @principle Requires authentication for data access.
     */
    match /companies/{companyId}/heldOrders/{orderId} {
        function isSignedIn() {
            return request.auth != null;
        }
        allow get: if isSignedIn();
        allow list: if isSignedIn();
        allow create: if isSignedIn();
        allow update: if isSignedIn();
        allow delete: if isSignedIn();
    }
  }
}