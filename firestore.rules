/**
 * @fileoverview Firestore Security Rules for Zenith POS.
 *
 * Core Philosophy:
 * This ruleset enforces a strict company-based data isolation model. Users can only
 * access data within their assigned company.  User profiles are private and
 * accessible only to the owning user.
 *
 * Data Structure:
 * - /users/{userId}: Stores individual user profiles.
 * - /companies/{companyId}: Stores company-level data. All other data is nested
 *   under a company.
 * - /companies/{companyId}/...:  All company-specific data (customers, suppliers,
 *   items, sales, etc.) is stored under the respective company document.
 *
 * Key Security Decisions:
 * - Users can only list data within their company.
 * - User listing is disallowed.
 * - Self-creation is allowed for user documents.
 * - Ambiguous relationships default to owner-only access.
 *
 * Denormalization for Authorization:
 *  No denormalization is required as the `companyId` is present on the `User` entity
 *  and can be used to authorize operations on company-scoped collections.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Grants access to user profile information.
     * @path /users/{userId}
     * @allow (create) User with matching userId can create their profile.
     * @allow (get, update, delete) User with matching userId can get, update, and delete their profile.
     * @deny (list) User listing is not permitted.
     * @deny (create, update, delete) Non-matching userIds cannot create, update, or delete this profile.
     * @principle Enforces document ownership for user profiles.
     */
    match /users/{userId} {
      function isSignedIn() {
        return request.auth != null;
      }
      function isOwner(userId) {
        return request.auth.uid == userId;
      }
      function isExistingOwner(userId) {
          return isOwner(userId) && resource != null;
      }

      allow create: if isSignedIn() && isOwner(userId) && request.resource.data.id == userId;
      allow get: if isOwner(userId);
      allow update: if isExistingOwner(userId) && request.resource.data.id == resource.data.id;
      allow delete: if isExistingOwner(userId);
      allow list: if false;
    }

    /**
     * @description Grants access to company information.
     * @path /companies/{companyId}
     * @allow (get) Any signed-in user can read company information.
     * @allow (create, update, delete) No one can create, update, or delete company information through the client.
     * @deny (list) Company listing is not permitted.
     * @principle Restricts company creation, update, and deletion to backend services.
     */
    match /companies/{companyId} {
      function isSignedIn() {
        return request.auth != null;
      }
      allow get: if isSignedIn();
      allow create: if false;
      allow update: if false;
      allow delete: if false;
      allow list: if false;
    }

    /**
     * @description Grants access to customer data for a specific company.
     * @path /companies/{companyId}/customers/{customerId}
     * @allow (get, list) Any signed-in user can read/list customers for their company.
     * @allow (create) Any signed-in user can create customers for their company.
     * @allow (update, delete) Any signed-in user can update/delete customers for their company.
     * @deny (create, update, delete) Signed-in user cannot perform operations outside their company.
     * @principle Enforces company-based data isolation for customers.
     */
    match /companies/{companyId}/customers/{customerId} {
      function isSignedIn() {
        return request.auth != null;
      }
      function isMemberOfCompany(companyId) {
        return isSignedIn() && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.companyId == companyId;
      }
        function isExistingMemberOfCompany(companyId) {
          return isMemberOfCompany(companyId) && resource != null;
      }

      allow get: if isMemberOfCompany(companyId);
      allow list: if isMemberOfCompany(companyId);
      allow create: if isMemberOfCompany(companyId);
      allow update: if isExistingMemberOfCompany(companyId);
      allow delete: if isExistingMemberOfCompany(companyId);
    }

    /**
     * @description Grants access to supplier data for a specific company.
     * @path /companies/{companyId}/suppliers/{supplierId}
     * @allow (get, list) Any signed-in user can read/list suppliers for their company.
     * @allow (create) Any signed-in user can create suppliers for their company.
     * @allow (update, delete) Any signed-in user can update/delete suppliers for their company.
     * @deny (create, update, delete) Signed-in user cannot perform operations outside their company.
     * @principle Enforces company-based data isolation for suppliers.
     */
    match /companies/{companyId}/suppliers/{supplierId} {
      function isSignedIn() {
        return request.auth != null;
      }
      function isMemberOfCompany(companyId) {
        return isSignedIn() && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.companyId == companyId;
      }

      function isExistingMemberOfCompany(companyId) {
        return isMemberOfCompany(companyId) && resource != null;
      }

      allow get: if isMemberOfCompany(companyId);
      allow list: if isMemberOfCompany(companyId);
      allow create: if isMemberOfCompany(companyId);
      allow update: if isExistingMemberOfCompany(companyId);
      allow delete: if isExistingMemberOfCompany(companyId);
    }

    /**
     * @description Grants access to item categories for a specific company.
     * @path /companies/{companyId}/categories/{categoryId}
     * @allow (get, list) Any signed-in user can read/list categories for their company.
     * @allow (create) Any signed-in user can create categories for their company.
     * @allow (update, delete) Any signed-in user can update/delete categories for their company.
     * @deny (create, update, delete) Signed-in user cannot perform operations outside their company.
     * @principle Enforces company-based data isolation for item categories.
     */
    match /companies/{companyId}/categories/{categoryId} {
      function isSignedIn() {
        return request.auth != null;
      }
      function isMemberOfCompany(companyId) {
        return isSignedIn() && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.companyId == companyId;
      }
      function isExistingMemberOfCompany(companyId) {
        return isMemberOfCompany(companyId) && resource != null;
      }

      allow get: if isMemberOfCompany(companyId);
      allow list: if isMemberOfCompany(companyId);
      allow create: if isMemberOfCompany(companyId);
      allow update: if isExistingMemberOfCompany(companyId);
      allow delete: if isExistingMemberOfCompany(companyId);
    }

    /**
     * @description Grants access to items for a specific company.
     * @path /companies/{companyId}/items/{itemId}
     * @allow (get, list) Any signed-in user can read/list items for their company.
     * @allow (create) Any signed-in user can create items for their company.
     * @allow (update, delete) Any signed-in user can update/delete items for their company.
     * @deny (create, update, delete) Signed-in user cannot perform operations outside their company.
     * @principle Enforces company-based data isolation for items.
     */
    match /companies/{companyId}/items/{itemId} {
      function isSignedIn() {
        return request.auth != null;
      }
      function isMemberOfCompany(companyId) {
        return isSignedIn() && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.companyId == companyId;
      }
      function isExistingMemberOfCompany(companyId) {
        return isMemberOfCompany(companyId) && resource != null;
      }

      allow get: if isMemberOfCompany(companyId);
      allow list: if isMemberOfCompany(companyId);
      allow create: if isMemberOfCompany(companyId);
      allow update: if isExistingMemberOfCompany(companyId);
      allow delete: if isExistingMemberOfCompany(companyId);
    }

    /**
     * @description Grants access to tables for a specific company.
     * @path /companies/{companyId}/tables/{tableId}
     * @allow (get, list) Any signed-in user can read/list tables for their company.
     * @allow (create) Any signed-in user can create tables for their company.
     * @allow (update, delete) Any signed-in user can update/delete tables for their company.
     * @deny (create, update, delete) Signed-in user cannot perform operations outside their company.
     * @principle Enforces company-based data isolation for tables.
     */
    match /companies/{companyId}/tables/{tableId} {
      function isSignedIn() {
        return request.auth != null;
      }
      function isMemberOfCompany(companyId) {
        return isSignedIn() && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.companyId == companyId;
      }
      function isExistingMemberOfCompany(companyId) {
        return isMemberOfCompany(companyId) && resource != null;
      }

      allow get: if isMemberOfCompany(companyId);
      allow list: if isMemberOfCompany(companyId);
      allow create: if isMemberOfCompany(companyId);
      allow update: if isExistingMemberOfCompany(companyId);
      allow delete: if isExistingMemberOfCompany(companyId);
    }

    /**
     * @description Grants access to sales data for a specific company.
     * @path /companies/{companyId}/sales/{saleId}
     * @allow (get, list) Any signed-in user can read/list sales for their company.
     * @allow (create) Any signed-in user can create sales for their company.
     * @allow (update, delete) Any signed-in user can update/delete sales for their company.
     * @deny (create, update, delete) Signed-in user cannot perform operations outside their company.
     * @principle Enforces company-based data isolation for sales.
     */
    match /companies/{companyId}/sales/{saleId} {
      function isSignedIn() {
        return request.auth != null;
      }
      function isMemberOfCompany(companyId) {
        return isSignedIn() && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.companyId == companyId;
      }
      function isExistingMemberOfCompany(companyId) {
        return isMemberOfCompany(companyId) && resource != null;
      }

      allow get: if isMemberOfCompany(companyId);
      allow list: if isMemberOfCompany(companyId);
      allow create: if isMemberOfCompany(companyId);
      allow update: if isExistingMemberOfCompany(companyId);
      allow delete: if isExistingMemberOfCompany(companyId);
    }

    /**
     * @description Grants access to payment methods for a specific company.
     * @path /companies/{companyId}/paymentMethods/{methodId}
     * @allow (get, list) Any signed-in user can read/list payment methods for their company.
     * @allow (create) Any signed-in user can create payment methods for their company.
     * @allow (update, delete) Any signed-in user can update/delete payment methods for their company.
     * @deny (create, update, delete) Signed-in user cannot perform operations outside their company.
     * @principle Enforces company-based data isolation for payment methods.
     */
    match /companies/{companyId}/paymentMethods/{methodId} {
      function isSignedIn() {
        return request.auth != null;
      }
      function isMemberOfCompany(companyId) {
        return isSignedIn() && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.companyId == companyId;
      }
      function isExistingMemberOfCompany(companyId) {
        return isMemberOfCompany(companyId) && resource != null;
      }

      allow get: if isMemberOfCompany(companyId);
      allow list: if isMemberOfCompany(companyId);
      allow create: if isMemberOfCompany(companyId);
      allow update: if isExistingMemberOfCompany(companyId);
      allow delete: if isExistingMemberOfCompany(companyId);
    }

    /**
     * @description Grants access to VAT rates for a specific company.
     * @path /companies/{companyId}/vatRates/{vatId}
     * @allow (get, list) Any signed-in user can read/list VAT rates for their company.
     * @allow (create) Any signed-in user can create VAT rates for their company.
     * @allow (update, delete) Any signed-in user can update/delete VAT rates for their company.
     * @deny (create, update, delete) Signed-in user cannot perform operations outside their company.
     * @principle Enforces company-based data isolation for VAT rates.
     */
    match /companies/{companyId}/vatRates/{vatId} {
      function isSignedIn() {
        return request.auth != null;
      }
      function isMemberOfCompany(companyId) {
        return isSignedIn() && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.companyId == companyId;
      }
        function isExistingMemberOfCompany(companyId) {
          return isMemberOfCompany(companyId) && resource != null;
      }

      allow get: if isMemberOfCompany(companyId);
      allow list: if isMemberOfCompany(companyId);
      allow create: if isMemberOfCompany(companyId);
      allow update: if isExistingMemberOfCompany(companyId);
      allow delete: if isExistingMemberOfCompany(companyId);
    }

    /**
     * @description Grants access to held orders for a specific company.
     * @path /companies/{companyId}/heldOrders/{orderId}
     * @allow (get, list) Any signed-in user can read/list held orders for their company.
     * @allow (create) Any signed-in user can create held orders for their company.
     * @allow (update, delete) Any signed-in user can update/delete held orders for their company.
     * @deny (create, update, delete) Signed-in user cannot perform operations outside their company.
     * @principle Enforces company-based data isolation for held orders.
     */
    match /companies/{companyId}/heldOrders/{orderId} {
      function isSignedIn() {
        return request.auth != null;
      }
      function isMemberOfCompany(companyId) {
        return isSignedIn() && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.companyId == companyId;
      }
        function isExistingMemberOfCompany(companyId) {
          return isMemberOfCompany(companyId) && resource != null;
      }

      allow get: if isMemberOfCompany(companyId);
      allow list: if isMemberOfCompany(companyId);
      allow create: if isMemberOfCompany(companyId);
      allow update: if isExistingMemberOfCompany(companyId);
      allow delete: if isExistingMemberOfCompany(companyId);
    }
  }
}